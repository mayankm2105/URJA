# Version floors (was exact-pinned) so deps resolve on newer Python (incl. 3.14),
# where pydantic-core/sqlalchemy 2024 pins have no prebuilt wheels. Same libraries.
fastapi>=0.111
uvicorn[standard]>=0.29
sqlalchemy>=2.0.30
pydantic>=2.9
python-dotenv>=1.0.1
httpx>=0.27
