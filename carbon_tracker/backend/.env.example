# Gemini API Key for Live AI Advisor chat grounding (Optional)
GEMINI_API_KEY=your_gemini_api_key_here

# Comma-separated list of allowed CORS origins (Optional, defaults to localhost origins if empty)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
